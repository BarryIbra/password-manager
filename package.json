{
    "name": "password-manager",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix front\" \"npm run start --prefix electron\"",
        "build": "npm --prefix electron run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "electron-builder": "^26.0.12"
    }
}
{
    "name": "password-manager",
    "scripts": {
        "dev": "concurrently \"npm --prefix front run dev \" \"npm --prefix electron run start \"",
        "build:front": "npm --prefix front run build",
        "build:electron": "npm --prefix electron run build",
        "build": "npm run build:front && npm run build:electron"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "electron-builder": "26.0.12"
    }
}